# =============================================================================
# India Bot — Requirements
# Install: pip install -r requirements.txt
# =============================================================================

# Zerodha Kite Connect (for order placement in live mode only)
kiteconnect>=5.0.0

# Environment variable management
python-dotenv>=1.0.0

# Data processing
pandas>=2.0.0
numpy>=1.24.0

# Technical indicators (Supertrend, RSI, EMA)
pandas_ta>=0.3.14b

# Market data — Yahoo Finance (free, used for daily candles)
yfinance>=0.2.40

# HTTP requests (Telegram notifications)
requests>=2.31.0